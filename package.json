{
  "name": "foodloop-ai",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build:vercel",
    "dev": "cd client && npm run dev",
    "start": "node api/index.js"
  }
}